/**
 * Products feature — public API.
 *
 * Pages and other features import from `@/features/products` and never reach
 * into subfolders, so the internal layout stays free to change.
 */
export * from './components'

VITE_API_BASE_URL=http://localhost:3000/api
VITE_APP_NAME=ShopSphere

# Stripe publishable key (safe to expose). Optional — falls back to GET /payments/config.
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_xxx
